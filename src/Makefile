OBJS=  mainProgram.o
CFLAGS=-g -O3
all: runner
runner: $(OBJS) 
	cc -o runner $(CFLAGS) $(OBJS)

# all object files built from c files that include draw.h
$(OBJS): tokenizer.h history.h

# deletes files generated by compilation
clean:
	rm -f *.o runner

run:runner
	./runner
